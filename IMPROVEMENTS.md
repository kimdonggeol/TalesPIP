# Tales PIP 개선사항 리스트

## 완료 (표기 정리 / 불필요한 알림 제거)
- [x] 설정 창에서 프로세스 이름(`InphaseNXD.exe`) 노출 제거 → `테일즈위버 연결됨` / `테일즈위버 실행 대기 중`
- [x] 트레이 툴팁·안내 대화상자도 동일하게 표시명 사용 (`TARGET_LABEL` 상수로 분리)
- [x] `TARGET_PROCESS`는 내부 프로세스 매칭에만 사용, UI 문구에서 완전히 제거
- [x] "대상 프로그램을 찾았지만 창을 특정할 수 없습니다" 알림 제거 — 프로그램 지정 기능이 없어진 뒤로는 안내할 동작 자체가 없었음

## 완료 (알림 on/off)
- [x] 트레이 알림 표시 여부 설정 추가 (기본 켬)
- [x] 모든 알림을 `notify()` 헬퍼로 일원화 — 해상도 전환, 빈 프리셋 전환, 단축키 등록 실패, 창 특정 실패

## 완료 (해상도 전환 시 위치 이중 적용)
- [x] 해상도 변경으로 프리셋이 자동 전환될 때 PIP 위치가 어긋나던 문제 수정
- [x] 원인: 전환 직후 `apply_stored_geometry()`가 **새 원점 기준**으로 정확히 배치한 뒤, 같은 틱에서 `move_pips_by(dx, dy)`가 보이는 PIP를 **다시 델타만큼 이동**시켜 이중 적용됨
- [x] 크기 변경이 감지된 경우엔 델타 이동 대신 저장된 오프셋으로 재계산하도록 분기 (이동만 발생한 경우엔 기존 델타 방식 유지 — 드래그 추적이 부드러움)
- [x] "대상 창을 따라 이동"이 꺼져 있으면 크기 변경 시 위치를 건드리지 않음

## 완료 (영역 미리보기 / 미등록 해상도 처리)
- [x] 설정 창에서 PIP를 선택하면 해당 영역의 **실시간 미리보기** 표시
- [x] DWM 썸네일을 설정 창 자체에 등록하고 미리보기 박스 위치에 렌더링 (썸네일은 창의 위젯 위에 그려지므로 박스는 자리만 확보하는 용도)
- [x] 원본 비율 유지(레터박스) + 원본 픽셀 크기 표시
- [x] 창 크기 변경·선택 변경 시 위치 갱신, 창을 닫거나 숨기면 썸네일 해제
- [x] 미등록 해상도로 바뀌면 **비어 있는 다음 프리셋으로 전환**하고 그 해상도를 기록 — 다른 해상도의 영역이 잘못된 위치에 남아 있는 것보다 나음
- [x] 비어 있는 프리셋이 없으면 현재 프리셋 유지
- [x] 전환 시 트레이 알림으로 안내

## 완료 (PIP 목록 가시성 / 삭제 확인 제거)
- [x] PIP 목록이 너무 짧아 스크롤해야 하던 문제 개선 — 좌측 패널에 카드 5개가 쌓여 목록이 두어 줄로 눌리고 있었음
- [x] 목록 최소 높이 220px 확보, 설정 창 기본 크기 820x600 → 880x780
- [x] 좌측 패널을 스크롤 영역으로 감싸 창을 줄여도 모든 항목에 접근 가능하도록 함 (다크 테마 QSS 적용)
- [x] 영역 삭제 시 확인 창 제거 — 즉시 삭제

## 완료 (영역 지정 후 포커스)
- [x] 설정 창에서 영역을 추가/수정한 직후 설정 창이 다시 앞으로 올라와 새 PIP와 게임 화면을 가리던 문제 수정
- [x] 선택 창이 닫힌 직후 대상 게임 창을 포그라운드로 올림 (`focus_target()`)
- [x] 이 시점엔 우리 앱이 포그라운드라 포커스 이양이 허용됨 — 백그라운드에서 호출했다면 윈도우가 막았을 동작
- [x] 영역 추가·수정 양쪽 경로에 적용

## 완료 (커서 대응 최종: PIP 부분 투명화)
- [x] 결론: 게임이 커서를 프레임에 렌더링해버려 **커서 픽셀과 배경을 분리할 마스크가 없음** → 사각형으로 오려내는 방식은 커서 크기가 바뀔 때마다 잘리거나 배경이 붙는 문제를 근본적으로 피할 수 없음
- [x] 방식 변경: 커서가 마우스 통과 PIP 위에 올라가면 **그 PIP를 일시적으로 흐리게** 처리해 아래 게임 화면과 게임 커서가 그대로 비치게 함
- [x] 사각 패치 없음, 잘림 없음, 커서 종류와 무관
- [x] 흐림 정도를 설정에서 조절 (기본 60% — 25%는 너무 흐려 가독성이 떨어져 상향)
- [x] 기존 config에 저장된 옛 기본값 25는 로드 시 자동으로 60으로 갱신
- [x] 커서가 벗어나면 원래 투명도로 복귀 (영역별 반투명 설정과 함께 적용 — 둘 중 더 흐린 값 사용)
- [x] 사용하지 않게 된 커서 캡처(`GetCursorInfo`/`DrawIconEx`)·미러 패치 코드 및 관련 win32 구조체 전부 제거

## (폐기) 게임 자체 커서 미러링
- [x] 게임이 시스템 커서를 숨기고 씬에 직접 그리는 경우 대응 (`CURSOR_SHOWING` 꺼짐 → 복사할 HCURSOR 자체가 없음)
- [x] `CursorMirror` 추가 — 커서 주변 44x44 영역을 DWM으로 1:1 미러링해 **게임이 그린 커서를 그대로** 표시
- [x] 소스 박스를 클라이언트 영역 안으로 클램프하고 창을 그 박스의 실제 화면 좌표에 배치 → 화면 가장자리에서도 1:1 정렬 유지
- [x] 시스템 커서가 있으면 기존 HCURSOR 복사 방식, 없으면 자동으로 미러 방식으로 전환
- [x] 미러 창은 반투명 미적용 — DWM 썸네일은 레이어드 창에 합성되지 않음
- [ ] 트레이드오프: 커서 주변 게임 배경도 같이 비치는 사각 패치로 보임 (사용자 확인 필요)

## 완료 (실제 커서 미러링)
- [x] PIP 위 커서를 직접 그린 화살표 → **실제 시스템 커서 그대로** 표시
- [x] `GetCursorInfo`로 현재 커서 핸들을 얻고 `GetIconInfo` + `DrawIconEx`로 DIB에 렌더링해 QImage로 변환
- [x] 게임이 `SetCursor`로 지정한 커스텀 커서(칼/손 모양 등)도 그대로 반영됨
- [x] 핫스팟 보정 — 커서 이미지의 기준점을 실제 커서 위치에 맞춤
- [x] 커서 핸들이 바뀔 때만 재렌더링 (애니메이션 커서도 자동 갱신, 25ms 폴링 부하 최소화)
- [x] 게임이 시스템 커서를 숨기고 씬 안에 직접 그리는 경우엔 기존 화살표로 폴백
- [ ] 사용자 확인 필요: 게임이 Win32 커서를 쓰는지 씬에 직접 그리는지에 따라 결과가 달라짐

## 완료 (프리셋 전환 위치 / 커서 표시)
- [x] 프리셋 전환 후 PIP 위치가 어긋나던 문제 수정
- [x] 원인: 위치는 대상 클라이언트 원점 기준 상대 좌표로 저장되지만 **창 생성 시 한 번만** 절대 좌표로 변환됨. 숨겨진 PIP는 대상 이동을 따라가지 않으므로(`move_pips_by`는 보이는 창만 이동) 다시 표시될 때 옛 좌표에 나타남
- [x] `apply_stored_geometry()` 추가 — 표시 직전에 저장된 오프셋으로부터 절대 좌표를 재계산
- [x] 마우스 통과 PIP 위에 **커서 오버레이** 추가 — 게임은 자체 커서를 창 안에 렌더링하므로 PIP가 덮은 영역에선 커서가 보이지 않았음
- [x] 커서가 마우스 통과 PIP 위에 있을 때만 표시, 25ms 주기로 추적, 입력은 그대로 통과
- [x] 설정에 "마우스 통과 PIP 위에 커서 표시" 토글 추가 (기본 켬)
- [ ] 검증 한계: 이 환경에서는 `SetCursorPos`가 실제 커서를 움직이지 못해, 커서 좌표를 주입해 5개 시나리오를 검증함

## 완료 (UI 클리핑 / 설정 초기화)
- [x] "현재 해상도로 지정" 버튼 텍스트가 `재 해상도로 지`로 잘리던 문제 수정 — 300px 좌측 패널에 버튼 2개를 가로로 배치해 생긴 클리핑
- [x] 두 버튼을 세로로 분리하고 좌측 패널 폭 300 → 340px로 확대, 문구도 명확하게 변경
- [x] 설정 창 전체 위젯의 실제 폭과 `sizeHint()` 비교 검사 — 잘리는 위젯 0개 확인
- [x] `config.json` 초기화 (기존 파일은 `config.json.old`로 백업)

## 완료 (프리셋 = 해상도 프로필로 재설계)
- [x] 개념 변경: 프리셋은 **대상 프로그램의 해상도 프로필**. 크롭 좌표는 %로 저장되지만 게임 UI는 해상도에 비례해 움직이지 않으므로, 해상도가 다르면 같은 %가 다른 곳을 가리킴 → 해상도별로 영역 세트를 분리
- [x] 워크플로 반전: 영역에 프리셋을 배정하는 방식 → **프리셋을 먼저 고르고 그 안에 영역을 추가**
- [x] 영역은 하나의 프리셋에만 소속 (`region["preset"]`, 정수)
- [x] 프리셋마다 이름 + 기준 해상도 저장. 첫 영역 추가 시 당시 해상도를 자동 기록
- [x] **해상도 변경 감지 시 일치하는 프리셋으로 자동 전환** (트레이 알림 표시, 설정에서 끌 수 있음)
- [x] 일치하는 프리셋이 없으면 현재 프리셋 유지 (임의 전환 안 함)
- [x] 같은 해상도를 두 프리셋에 지정하려 하면 차단
- [x] 단축키 동작 정리: 다른 프리셋 키 → 전환 / 현재 프리셋 키 → PIP 숨김 토글
- [x] "전체 표시" 개념 제거 — 해상도가 안 맞는 영역까지 함께 띄우면 잘못된 위치를 보여주게 됨
- [x] 설정 UI 재구성: 프리셋 선택/이름/해상도 카드 + 해당 프리셋의 PIP 목록, 영역 상세엔 "소속 프리셋" 이동 콤보
- [x] 트레이 메뉴에 프리셋 4개 라디오 표시 + "PIP 숨기기" 토글
- [x] 기존 config 자동 마이그레이션: 영역의 `presets` 배열 → 단일 `preset` (가장 낮은 번호 채택)

## 완료 (기능)
- [x] 캡처 방식 GDI(PrintWindow) → DWM 썸네일 (GPU 렌더링 앱 대응, 화면 재귀 원천 차단)
- [x] 자동 hwnd 추정 제거, 사용자 수동 선택으로 고정
- [x] 선택 창을 대상 클라이언트 영역에 1:1 오버레이 (실측 검증: 1920x1200 완전 일치)
- [x] 드래그 시 검은 화면 → 썸네일 유지 + WindowTransparentForInput 테두리 창
- [x] PIP 창 8방향 테두리 리사이즈
- [x] 설정 UI (다크 테마), 트레이 아이콘
- [x] 마우스 통과(click-through), 반투명 슬라이더, 위치/크기 수치 입력

## 완료 (마우스 통과 / 프리셋 표기 / 단축키 기본값)
- [x] PIP 우클릭 메뉴에 "마우스 통과" 체크 항목 추가 (현재 상태가 체크로 표시됨)
- [x] 프리셋 배정 변경 시 PIP 목록이 즉시 갱신되지 않던 문제 수정 — 체크박스 시그널 안에서 목록 전체를 재구성하면 선택이 꼬이므로 해당 행 텍스트만 갱신
- [x] 목록 표기를 `[1]` → **`프리셋 1`** / `프리셋 1, 3` / `프리셋 없음` 으로 명확화
- [x] 프리셋 단축키 기본값을 **Ctrl+F9~F12**로 변경, 선택 목록에 Ctrl+F1~F12 추가
- [x] 이전 버전 기본값(`Alt+FN`, `Ctrl+Alt+F1~F4`)은 새 기본값으로 자동 이전, **사용자가 직접 바꾼 값은 유지**

## 완료 (우클릭 메뉴 1회차 무반응)
- [x] PIP 우클릭 → 영역 추가를 1회 눌러도 반응 없고 두 번째에야 뜨던 문제 수정
- [x] 원인 1: `menu.exec()`의 중첩 이벤트 루프 **안에서** 시그널을 emit해 창을 띄우고 있었음 → 루프가 풀린 뒤 실행되도록 `QTimer.singleShot(0, ...)`으로 지연
- [x] 원인 2: `self.picker` 참조가 닫힌 뒤에도 남아 다음 호출의 중복 가드에 걸림. `destroyed` 시그널은 `WA_DeleteOnClose`가 없어 발생하지 않았음 → `closed` 시그널을 추가하고 `WA_DeleteOnClose` 설정
- [x] 추가/수정/삭제/설정 열기 **모든** 우클릭 항목에 동일 처리 적용
- [x] 이미 선택 창이 열려 있으면 중복 생성 없이 기존 창을 앞으로 가져옴

## 완료 (활성 창 연동)
- [x] 게임이 최상위(포그라운드) 창이 아닐 때 PIP 자동 숨김, 다시 활성화되면 복원
- [x] **자기 프로세스 창은 예외 처리** — PIP를 드래그하거나 설정 창을 열면 우리 앱이 포그라운드가 되므로, 이를 막지 않으면 PIP를 조작하는 순간 사라짐
- [x] 50ms 타이머에서 상태 변화 시에만 갱신 (매 틱 show/hide 호출 안 함)
- [x] 프리셋 필터와 독립적으로 동작 — 활성 상태 + 프리셋 조건을 모두 만족할 때만 표시
- [x] 설정에 "게임이 활성 창일 때만 표시" 토글 추가 (기본 켬)
- [ ] 검증 한계: 이 환경에서는 `SetForegroundWindow`로 실제 포커스를 뺏을 수 없어(윈도우의 포그라운드 탈취 제한), 포그라운드 판정 함수를 스텁으로 대체해 9개 시나리오를 검증함

## 완료 (선택 피드백 정리)
- [x] 드래그 중 픽셀 크기 표시 제거
- [x] 영역 생성 직후 해당 PIP 위치에 **일시적 강조 효과** 추가 — 3회 펄스 후 자연스럽게 사라짐
- [x] 영역 **수정** 후에도 동일한 강조 효과 표시
- [x] 깜빡임 속도 완화: 1.4초 → 2.6초, 펄스 간격 455ms → 845ms (1.9배 느리게)
- [x] 영역 수정 시 PIP가 기존 크기를 유지해 내용이 늘어지던 문제 수정 → **새로 지정한 영역 크기로 재설정** (위치는 유지)
- [x] 강조 효과는 별도 최상위 입력투과 창으로 구현 — DWM 썸네일이 PIP 자체 위젯을 덮어 그리므로 내부 위젯으로는 표시 불가
- [x] 영역 이름을 강조 효과 중앙에 표시 (PIP가 너무 작으면 생략)
- [x] 효과 종료 시 자동 정리 (`WA_DeleteOnClose`) — 참조 누수 없음

## 완료 (선택 창 추적)
- [x] 영역 선택 중 대상 창을 이동하면 선택 창(썸네일·테두리·배너)이 함께 따라 이동 (50ms 추적)
- [x] 드래그하는 도중에 대상이 이동해도 선택 사각형이 같은 위치를 유지하도록 로컬 좌표 기준으로 재배치
- [x] 선택 중 대상 창이 닫히면 선택 창 자동 종료
- [x] 대상이 최소화된 동안에는 추적을 건너뜀 (화면 밖 -32000 좌표로 끌려가는 것 방지)

## 완료 (영역 선택 UI / PIP 크기 제한)
- [x] 선택 모드임을 알 수 없던 문제 수정. 원인: `activateWindow()`가 picker를 안내 배너 **위로** 올려 배너가 가려지고 있었음
- [x] 화면 전체를 감싸는 파란 테두리(`SelectionFrame`) 추가 — 선택 중 상태가 한눈에 구분됨
- [x] 배너를 "영역 추가 중 / 영역 수정 중" + 부제(드래그 안내·ESC 취소)로 개편, 녹화 표시용 빨간 점 추가
- [x] 드래그 중 배너가 사라지던 동작 제거 — 선택 내내 모드가 계속 보임
- [x] 드래그 중 **실시간 크기 표시**(실제 픽셀 단위) 칩 추가 — 원하는 크기를 정확히 맞출 수 있음
- [x] HUD가 picker에 가려지지 않도록 z-order 재적용(`SetWindowPos` HWND_TOPMOST) + 위치 재보정
- [x] HUD 위치를 네이티브 좌표로 배치 — Qt 논리좌표 경유 시 DPI 배율에서 1px 어긋남
- [x] PIP 최소 크기 80x60 → **8x8** (사실상 제한 없음)
- [x] 작은 PIP에서 테두리 리사이즈 영역이 창 전체를 덮어 드래그 불가하던 문제 → 마진을 크기에 비례해 축소
- [x] PIP 최대 크기를 **화면 해상도보다 작게** 제한 (실측 3072x1232 화면 → 최대 3064x1224)
- [x] 설정 창 크기 입력란 범위도 동일하게 적용

## 완료 (종료 반응 속도)
- [x] 프로세스 종료 후 PIP가 꺼지기까지 최대 1초+ 걸리던 지연 해소 → **실측 9ms**
- [x] 원인: 종료 감지가 1초 주기 `psutil` 프로세스 스캔에만 의존했음
- [x] 창 소멸(`IsWindow`)을 50ms 타이머에서 감지해 즉시 숨김 — 창이 사라지는 건 프로세스 종료보다 먼저 관측됨
- [x] `is_target_running()`이 살아있는 창이 있으면 전체 프로세스 스캔을 건너뛰도록 최적화 (2.8ms → 0.001ms)
- [x] 숨김 처리를 `on_target_lost()`로 통합 (중복 호출 안전)

## 완료 (InphaseNXD 전용화 / 최초 생성 위치)
- [x] 영역 최초 생성 시 PIP가 **드래그한 그 위치**에 생성되도록 수정 (기존엔 항상 100,100 고정값)
- [x] 프로그램 선택 기능 제거 — InphaseNXD.exe 전용으로 자동 탐지
- [x] 패처/메시지박스(`#32770` 대화상자 클래스) 제외 후 클라이언트 면적이 가장 큰 창을 선택
- [x] `WindowPickerDialog` 클래스, 트레이/우클릭 "프로그램 지정·대상 창 재선택" 메뉴, 설정 창 버튼 제거
- [x] 사용하지 않게 된 `target_process`/`target_window_title`/`target_window_class` 설정 키 자동 정리
- [x] 설정 창은 연결 상태만 표시 ("연결됨 — 자동 감지" / "실행 대기 중")

## 완료 (설정 창 동기화)
- [x] PIP 우클릭으로 영역 삭제 시 설정 창 목록이 갱신되지 않던 문제 수정
- [x] 원인: 설정 창의 삭제 버튼만 `refresh()`를 호출했고, PIP 우클릭은 컨트롤러의 `delete_region`을 직접 호출해 갱신 경로가 없었음
- [x] `refresh_settings()` 헬퍼를 만들어 삭제/추가/영역수정 **모든 경로**에서 설정 창 동기화 (우클릭 추가·수정도 동일 문제였음)

## 완료 (좌표계 / 단축키 개편)
- [x] 위치 X/Y를 모니터 절대좌표 → **대상 프로그램 클라이언트 좌상단(0,0) 기준 상대좌표**로 변경
- [x] 기존 절대좌표 config는 최초 연결 시 1회 자동 변환 (`pip_coords` 마커로 판별)
- [x] 마이그레이션 판정 버그 — 병합된 기본값이 항상 새 마커를 갖고 있어 플래그가 항상 False였음 → 파일 원본을 검사하도록 수정
- [x] 내부용 `_` 키가 config.json에 저장되지 않도록 제외
- [x] 드래그 크기 off-by-one — `QRect.width()`가 양 끝점을 포함해 영역이 1px씩 크게 잡히던 문제 수정 (실측: 드래그 500x375 → 영역 500x375 → PIP 500x375)
- [x] 기존 영역용 "선택 영역 크기로 맞추기 (1:1)" 버튼 추가 (예전에 만든 영역은 320x240 고정값을 유지하고 있었음)
- [x] 프리셋 단축키를 **Ctrl+Alt+F1~F4**로 변경 (F1~F12 선택 가능)
- [x] 기존 `Alt+FN` 설정은 `Ctrl+Alt+FN`으로 자동 승격, 과거 우회값 `Alt+F5`는 `Ctrl+Alt+F4`로 복원
- [x] Ctrl 조합이라 `Ctrl+Alt+F4` 정상 등록 확인 (단독 Alt+F4는 윈도우 예약이라 불가했음)
- [x] **중복 실행 방지** 추가 — 인스턴스 2개가 뜨면 단축키를 서로 뺏고 PIP가 두 배로 생김 (실제로 2개 실행 중인 것을 발견)

## 완료 (표시 정확도)
- [x] "항상 위에 표시"를 항상 true로 고정하고 설정 UI에서 제거 (뒤로 가려지면 PIP가 무의미)
- [x] 영역 최초 지정 시 PIP 크기가 선택 영역과 픽셀 단위로 동일 (실측 768x300 = 768x300, 오차 0)
- [x] 원인 1: 기본 크기가 320x240 고정값이었음 → 선택 영역의 실제 픽셀 크기로 계산
- [x] 원인 2: 썸네일을 테두리만큼 inset해서 그려 내용이 2px씩 줄어들었음 → 클라이언트 전체를 채우도록 변경
- [x] DPI 배율 보정 — 물리 픽셀 기준 크기를 Qt 논리좌표로 환산해 저장

## 완료 (대상 창 추적)
- [x] 대상 창이 이동하면 PIP도 같은 양만큼 따라 이동 (창모드 드래그 대응)
- [x] 전용 50ms 타이머로 추적 — 썸네일 갱신 주기(300ms)로는 드래그 시 눈에 띄게 밀림
- [x] 물리 픽셀 기준 `SetWindowPos`로 이동 — Qt 논리좌표(÷DPR) 누적 반올림 오차 회피
- [x] 이동이 멈춘 뒤(600ms 디바운스)에만 config 저장 — 드래그 중 매 프레임 파일 쓰기 방지
- [x] 대상 창 크기 변경 시 즉시 썸네일 재프레이밍 (크롭은 % 기반이라 내용은 자동 대응)
- [x] 설정에 "대상 창을 따라 이동" 토글 (기본 켬)
- [x] 재연결 시 기준점 초기화 — 재부착만으로 PIP가 튀지 않음
- [ ] 실기 검증 한계: 게임 창이 프로그램적 이동(`SetWindowPos`)에 반응하지 않아, 제어 가능한 더미 창으로만 검증함. 수동 드래그는 동일 경로(클라이언트 rect 변화 감지)이므로 동작 예상

## 완료 (백그라운드 상주 / 자동 재연결)
- [x] 트레이 상주. 창을 모두 닫아도 종료되지 않음 (`setQuitOnLastWindowClosed(False)`)
- [x] 대상 프로세스 종료 시 PIP 자동 숨김 + 썸네일 핸들 해제, 앱은 트레이에 유지
- [x] 대상 프로세스 재시작 시 **대화상자 없이 자동 재연결** 후 마지막 상태 복원
- [x] 마지막 선택 창을 클래스명 우선으로 기억(`target_window_class`) — 제목은 패치/로그인마다 바뀌므로 클래스 우선 매칭
- [x] 활성 프리셋을 config에 저장 → 앱 재시작/대상 재시작 후에도 동일 프리셋 유지
- [x] 프로세스는 떴는데 창이 아직 없는 경우 매 틱 재시도 (창 생성 지연 대응)
- [x] 창을 특정할 수 없으면(동일 프로세스 창이 여러 개) 추측하지 않고 트레이 알림으로 안내
- [x] 설정 창 자동 표시는 최초 실행(config 없음)에만 — 이후엔 조용히 트레이 상주
- [x] 트레이 툴팁에 현재 상태 표시 (대기 중 / 프리셋 N / 전체 표시)

## 완료 (프리셋 + 전역 단축키)
- [x] 프리셋 4개 지원. 단축키를 누르면 해당 프리셋의 PIP만 표시되고 나머지는 숨김
- [x] 같은 단축키를 다시 누르면 전체 표시로 복귀
- [x] PIP 하나를 여러 프리셋에 중복 배정 가능 (설정 UI 체크박스)
- [x] 프리셋별 단축키 Alt+F1~F12 중 자유 변경, "사용 안 함" 가능
- [x] 프리셋 간 단축키 중복 지정 차단
- [x] 다른 프로그램이 점유한 조합이면 등록 실패 감지 → 롤백 + 경고, 시작 시엔 트레이 알림
- [x] **Alt+F4는 윈도우 예약(창 닫기)이라 등록 불가** → 프리셋 4 기본값을 Alt+F5로 변경
- [x] Qt가 네이티브 필터를 이중 호출해 토글이 상쇄되던 버그 → 타임스탬프 중복 제거 + 이벤트 소비
- [x] 프리셋 활성 중 영역 추가 시 해당 프리셋에 자동 배정
- [x] 트레이 메뉴에 전체 표시 / 프리셋 1~4 항목 추가
- [x] 기존 config는 영역이 자동으로 프리셋 1에 배정되어 마이그레이션

## 완료 (QA 1팀 - 캡처/좌표/핸들)
- [x] P1 다중 모니터·혼합 DPI에서 picker 좌표 오류 → Qt 논리좌표 대신 `SetWindowPos` 네이티브 배치
- [x] P1 프로그램 선택 창이 1초마다 무한 중첩 → `was_running`을 dispatch 전에 대입 + `_busy` 재진입 가드
- [x] P1 최소화된 대상 → 화면 밖(-32000) 투명 창 생성 → `is_minimized` 가드 + 안내
- [x] P2 `setWindowFlags`가 WS_EX_TRANSPARENT 제거 → `apply_options()` 무조건 재적용
- [x] P2 대상 창만 닫힌 경우 썸네일 정지 → `check_process`에서 핸들 해제
- [x] P3 DPI 인식 PMv1 → PMv2(`SetProcessDpiAwarenessContext(-4)`) 우선 시도
- [x] P3 32비트 파이썬에서 `GetWindowLongPtrW` AttributeError → `getattr` 폴백
- [x] P3 테두리 inset 물리/논리 px 불일치 → `round(dpr)` 적용
- [x] `_dest_rect`를 실제 `GetClientRect` 기반으로 변경

## 완료 (QA 2팀 - UI/상태/설정)
- [x] HIGH 예외 시 매초 모달 오류창 폭주 → 대화상자 1회 제한(`log_exception(dialog=)`), 로그는 계속 기록
- [x] HIGH 트레이 불가/최초 실행 시 진입점 없음 → `isSystemTrayAvailable()` 확인 + 영역 0개면 설정 자동 표시
- [x] HIGH 이름 입력 중 Enter → 프로그램 선택창 뜸 → 모든 버튼 `setAutoDefault(False)`
- [x] HIGH 우클릭/단순 클릭마다 config 저장 + 입력 리셋 → 실제 이동/크기 변경 시에만 저장, 부분 갱신(`sync_geometry`)
- [x] MED 스핀박스 ±10000 제한이 다중 모니터 좌표 훼손 → `virtualGeometry` 기반 범위
- [x] MED `dict(DEFAULT_CONFIG)` 얕은 복사로 전역 오염 → `copy.deepcopy`
- [x] MED 스키마 마이그레이션 미흡(`KeyError: 'pip'`) → `load_config`에서 정규화/불량 항목 제거
- [x] MED 영역 0개일 때 전역 설정 접근 불가 → 좌측 패널로 이동
- [x] MED 대상 미지정 시 아무 반응 없음 → 안내 메시지
- [x] MED 이벤트 핸들러 내부에서 위젯 파괴 → `deleteLater()`
- [x] MED `save_config` 비원자적 → temp 파일 + `os.replace`
- [x] MED `_loading` 플래그 재진입 취약 → 카운터 + `try/finally`
- [x] MED 삭제 확인창이 PIP 뒤로 가림 → parent 지정
- [x] MED picker 중복 실행 → 가드 + `destroyed` 시 참조 해제
- [x] LOW QSS 깨진 리터럴(`#3d4椒a`) 및 런타임 `.replace()` 패치 제거
- [x] LOW 빈 이름 입력 시 필드 되돌림
- [x] LOW `PipWindow.resizeEvent`에 `super()` 호출 추가

## 미해결 / 후속 검토
- [ ] `dwm_unregister_thumbnail`의 HRESULT 미확인 (restype 미설정, 실패가 조용히 무시됨) — 핸들 재사용 시 다른 PIP 썸네일 파괴 가능. 현재는 해제 직후 None 대입 규율로 회피 중
- [ ] show 시 rebind 2회 중복 호출 (누수 아님, DWM 호출 낭비)
- [ ] 대상 프로세스 재시작 시 자동 재연결 없음 (수동 재지정 필요)
- [ ] 혼합 DPI 다중 모니터 실기 검증 미완 (현 환경 단일 모니터 125%)
- [ ] click_through 해제한 WS_EX_LAYERED 비트 원복 안 함 (렌더링 영향은 없음을 실측 확인)

## 보류 (요구사항 외)
- [ ] 단축키 지원
- [ ] 프로파일(설정 세트) 저장/전환
